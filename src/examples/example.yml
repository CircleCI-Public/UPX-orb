description: >
  Install UPX and pack an executable (compiled_binary).
usage:
  version: 2.1
  orbs:
    upx: gmem/upx@1.0.1
  jobs:
    build:
      docker:
        - image: cimg/go:1.14
      steps:
        - upx/install
        - upx/pack:
            binary: compiled_binary
